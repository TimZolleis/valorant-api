
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model Match {
 id String @id
 matchData Json
}

model Cache {
key String @id @unique
value String
}
